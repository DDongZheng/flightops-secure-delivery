# FlightOps Secure Delivery Factory — 安全开发政策

[English](SECURE_DEVELOPMENT_POLICY.md) | [Français](SECURE_DEVELOPMENT_POLICY.fr.md) | [中文](SECURE_DEVELOPMENT_POLICY.zh-CN.md)

## 1. 文档控制

| 字段 | 内容 |
| --- | --- |
| 版本 | 1.0 |
| 生效日期 | 2026-07-17 |
| 政策负责人 | Zheng Qianyuan |
| 复审频率 | 每季度，以及发生重大变更后 |
| 适用范围 | 应用代码、依赖、CI/CD 工作流和部署流程 |

## 2. 目的

本政策规定 FlightOps Secure Delivery Factory 应如何进行安全的设计、开发、测试、评审和发布。

本政策建立安全软件开发生命周期（SSDLC），要求安全与质量控制贯穿整个开发过程，而不是仅在生产发布前执行。

本政策支持与 ISO 27001 相关的实践，包括基于风险的变更控制、访问控制、安全开发、漏洞管理、环境隔离和证据保留。本政策本身并不代表项目已经获得 ISO 27001 认证。

## 3. 适用范围

本政策适用于：

- React 和 TypeScript 应用代码；
- 自动化测试和测试数据；
- npm 依赖及锁定文件；
- GitHub Actions 工作流；
- GitHub 仓库和分支规则；
- GitHub Environments；
- GitHub OIDC 与 AWS 的身份认证；
- 项目使用的 AWS 部署角色；
- AWS Amplify Staging 和 Production 部署；
- CI、安全、测试和部署证据；
- 使用 AI 辅助产生的贡献。

本应用是演示系统，只允许使用虚构且不敏感的数据。真实的航班、飞机、乘客、员工或运营信息不在批准范围内。

## 4. 政策用语

- **必须**：强制要求。
- **禁止**：不得执行的行为。
- **应该**：推荐要求，只有在理由被记录的情况下才能不执行。
- **可以**：可选实践。

## 5. 角色与职责

### 仓库所有者

仓库所有者必须：

- 维护本政策；
- 管理仓库和 Environment 的访问权限；
- 维护受保护分支和必要检查；
- 评审安全敏感变更；
- 批准或拒绝政策例外；
- 对已发现的漏洞进行分类和处理；
- 确保漏洞修复完成并得到验证；
- 审查安全和质量指标；
- 授权生产发布。

### 贡献者

贡献者必须：

- 通过独立分支和 Pull Request 工作；
- 遵守安全编码要求；
- 添加适当的自动化测试；
- 审查依赖变更；
- 处理 CI 和安全检查发现的问题；
- 避免提交密钥或敏感数据；
- 在 AI 生成的代码通过人工评审和测试前，将其视为不可信代码。

### 自动化平台控制

GitHub Actions 用于自动执行控制，但不能替代人的责任。当强制性的质量、安全、测试或部署要求未得到满足时，自动化控制必须明确失败。

### 职责分离限制

本项目是个人项目，因此仓库所有者可能同时担任开发者和生产审批人。对于当前演示范围，这项限制被接受。团队项目或真实生产系统应该要求安全敏感变更和生产发布由独立人员评审。

## 6. 安全软件开发生命周期

### 6.1 规划

开始实现前，每项变更应该明确：

- 变更目的和预期行为；
- 受影响的组件和环境；
- 是否引入新的数据、依赖、密钥或权限；
- 所需测试；
- 可能产生的安全影响；
- 验收标准。

如果变更引入身份认证、持久化存储、后端 API、个人数据或真实运营数据，必须在实施前重新进行安全评审。

### 6.2 设计

设计决策必须遵循以下原则：

- 最小权限；
- 最少数据收集；
- 安全默认值；
- Staging 与 Production 分离；
- 明确的信任边界；
- 可重复构建；
- 从源代码提交到部署版本的可追溯性；
- 纵深防御；
- 当验证失败时阻止不安全发布。

安全控制应该与组件的数据敏感度和业务影响相匹配。

### 6.3 实现

实现工作必须：

- 在专用分支上进行；
- 通过 Pull Request 提交；
- 保持范围清晰且便于评审；
- 在适用情况下使用 TypeScript 的类型安全能力；
- 验证不可信输入；
- 避免不安全的原始 HTML 渲染；
- 避免在错误信息中暴露密钥或内部实现细节；
- 为变更行为添加或更新测试；
- 避免引入无关的依赖或工作流变更。

正常开发流程禁止直接修改受保护的 `main` 分支。

### 6.4 验证

每个 Pull Request 在合并前必须通过所有适用的自动化控制：

- 使用 `npm ci` 进行可重复的依赖安装；
- ESLint；
- 单元测试和组件测试；
- 覆盖率门禁；
- Production Build；
- SonarQube 分析和 Quality Gate；
- CodeQL 分析；
- Dependency Review；
- `npm audit`；
- Gitleaks 密钥扫描；
- Playwright 端到端测试。

任何失败都必须进行调查。禁止仅为了获得通过结果而关闭或削弱检查。

### 6.5 发布

一次发布必须：

1. 来源于受保护的 `main` 分支；
2. 通过 Reusable CI 和 Security 工作流；
3. 将同一提交推进到固定的 `staging` 分支；
4. 显式触发 Amplify Staging 部署；
5. 验证 Amplify 部署状态和提交 ID；
6. 通过 Staging Smoke Test；
7. 进入受保护的 GitHub `production` Environment；
8. 获得已配置的 Production 审批；
9. 将同一发布提交部署到 Production；
10. 验证 Production 部署状态和提交 ID；
11. 通过 Production Smoke Test。

当 GitHub Actions 作为发布编排平台时，`main` 和 `staging` 的 Amplify Auto-build 必须保持关闭。

### 6.6 运行与维护

项目必须监控：

- 定时 Production Smoke Test；
- CI 和 Release 失败；
- Dependabot 更新；
- 定时安全扫描；
- SonarQube Quality Gate 结果；
- Amplify 部署结果；
- 尚未处理的漏洞。

依赖、GitHub Actions 和运行时版本应该在停止支持或出现明显过时问题前完成升级。

## 7. 源代码管理与评审

`main` 分支必须保持受保护状态。

Pull Request 必须描述变更内容、保持在可理解的范围内、通过必要检查、获得仓库规则要求的评审，并在合并前处理相关评审意见。

以下内容属于安全敏感变更：GitHub Actions 工作流、权限和密钥、OIDC 或 IAM 配置、GitHub Environment 配置、依赖、输入处理、身份认证和授权、部署逻辑以及安全扫描配置。

安全敏感变更应该接受额外的人工评审，重点检查权限变化、信任边界、绕过控制的可能性和意外信息泄露。

## 8. 质量要求

行为发生变化时，必须根据需要使用单元测试、组件测试、端到端测试和部署后的 Smoke Test。测试必须具有确定性，禁止依赖真实运营数据。

Statements、Branches、Functions 和 Lines 必须分别保持在配置的 80% 门槛以上。覆盖率只是最低门禁，并不能证明应用正确或安全。即使已经达到覆盖率数字，关键行为仍然必须得到测试。

在配置的 SonarQube Quality Gate 下，新代码禁止引入不可接受的可靠性、可维护性或安全问题。应该定期检查技术债和过时组件，并在它们产生明显的交付或安全风险前进行处理。

## 9. 应用安全要求

应用代码必须：

- 验证并规范化用户输入；
- 使用框架提供的输出转义；
- 避免使用 `dangerouslySetInnerHTML`，除非有单独理由并经过评审；
- 避免动态执行代码；
- 避免在前端构建产物中嵌入凭据或环境密钥；
- 避免记录敏感值；
- 处理错误时不得暴露密钥或内部安全细节；
- 使用安全且仍在维护的依赖。

由于当前应用公开且没有身份认证，因此必须仅使用虚构、存储于浏览器内存中的演示数据。

如果增加持久化、身份认证或后端服务，必须重新评审授权、会话管理、数据保护、日志、滥用防护和数据保留要求。

## 10. 密钥与凭据

密钥禁止：

- 提交到源代码仓库；
- 存储在应用源代码中；
- 放入公开文档；
- 暴露在前端构建变量中；
- 输出到工作流日志；
- 包含在测试报告或 Artifact 中；
- 提交给未经批准的 AI 工具。

Repository 和 Environment Secrets 必须限制在确实需要它们的工作流和环境中。

AWS 部署认证必须使用 GitHub OIDC 和临时凭据。禁止为日常部署引入长期 AWS Access Key。

如果密钥可能已经泄露，必须立即撤销或轮换，并检查相关历史记录、日志和 Artifact，同时记录该安全事件。

## 11. CI/CD 安全

工作流权限必须被显式声明、默认使用只读权限、仅向确实需要的 Job 授予写权限、仅向使用 OIDC 的 Job 授予 `id-token: write`，并限制在相关环境和操作范围内。

Staging 和 Production 必须使用不同的 Environment 配置和 AWS 角色。AWS 角色权限必须限制在目标 Amplify 分支所需的操作和资源范围内。

Release 工作流必须比较请求部署的提交 ID 和实际部署的提交 ID。两者不一致时必须判定发布失败。

第三方 GitHub Actions 必须来自可信且仍在维护的来源、使用明确版本、在首次引入或重大升级前接受评审，并使用最小权限运行。当需要更高等级的供应链保障时，应该考虑将高信任度的发布 Action 固定到完整 Commit SHA。

## 12. 依赖管理

自动化工作流必须通过已提交的 `package-lock.json` 和 `npm ci` 安装依赖。

依赖变更必须评审其必要性、维护状态、来源、发布者、已知漏洞、传递依赖影响、许可证影响及其与受支持 Node.js 版本的兼容性。

必须保持 Dependabot 对 npm 和 GitHub Actions 更新的配置。存在 High 或 Critical 依赖漏洞时，必须阻止正常发布，直到漏洞得到修复或获得有时间限制的正式例外。应该删除未使用的依赖。

## 13. 漏洞管理

安全问题可能来自 CodeQL、SonarQube、Dependency Review、`npm audit`、Dependabot、Gitleaks、人工代码评审、渗透测试、负责任的漏洞披露或生产环境观察。

每个确认的漏洞必须具有严重等级、负责人、处理决定、目标日期、修复证据和关闭前的验证结果。

| 严重等级 | 响应要求 |
| --- | --- |
| Critical | 立即分类和控制，目标在 72 小时内完成修复 |
| High | 目标在 7 个自然日内完成修复 |
| Medium | 目标在 30 个自然日内完成修复 |
| Low | 目标在 90 个自然日内修复，或在评审后接受风险 |

正在被利用的漏洞、凭据泄露或未经授权的生产访问，无论工具最初如何评级，都必须按照 Critical 处理。

禁止仅因为代码已经修改就关闭漏洞。必须通过重新扫描、自动化测试、人工评审或适当的安全复测验证修复结果。

## 14. 安全事件

怀疑发生安全事件时，仓库所有者必须：

1. 控制受影响的访问权限或发布路径；
2. 必要时停止不安全的部署；
3. 撤销或轮换受影响的凭据；
4. 保存相关日志和证据；
5. 确定受影响的提交、工作流和环境；
6. 修复根本原因；
7. 重新执行相关测试和安全检查；
8. 验证实际部署版本；
9. 记录经验教训和需要改进的控制。

恢复 Production 时不得绕过强制验证，除非已经记录并批准紧急例外。

## 15. AI 辅助开发

AI 工具可以辅助设计、实现、测试、文档和评审，但不得替代人的责任。

AI 工具使用者必须：

- 避免提交密钥、凭据或敏感运营数据；
- 遵守源代码保密要求和工具隐私设置；
- 在接受生成代码前进行人工评审；
- 验证 AI 建议的依赖和命令；
- 执行与人工代码相同的测试和安全控制；
- 检查不安全假设、虚构 API 和过度权限；
- 对最终变更承担责任。

AI 生成的代码在经过评审、测试和扫描前，必须被视为不可信的第三方输入。禁止使用 AI 输出绕过必要评审、Quality Gate 或 Production 审批。

## 16. 证据与指标

应该保留或引用以下证据：

- Pull Request 评审和检查结果；
- CI 和安全工作流日志；
- 覆盖率报告；
- SonarQube 结果；
- CodeQL 和依赖漏洞；
- Gitleaks 结果；
- Playwright 报告；
- GitHub Environment 审批历史；
- Amplify 部署 Job 记录；
- Staging 和 Production 提交验证；
- 漏洞修复记录。

政策负责人应该审查测试覆盖率、Quality Gate 状态、CI 和部署成功率、按严重等级和存在时间统计的未修复漏洞、漏洞修复时间、依赖状态、Production Smoke Test 失败、技术债和代码复杂度。

当前保留七天的 Artifact 可以提供短期执行证据。如果合同、监管或审计要求需要，应该增加长期证据保留。

## 17. 政策例外

只有当延迟变更会产生更大的、已记录的风险时，才允许绕过强制控制。

例外必须记录受影响的要求、原因、安全影响、补偿控制、负责人、批准记录、到期日期和后续行动。

例外必须具有时间限制，并在到期前重新评审。便利性或进度压力不能单独作为批准例外的理由。

## 18. 培训与持续改进

贡献者应该理解安全编码基础、密钥处理、依赖风险、CI/CD 权限、安全扫描结果的解释、漏洞修复和 AI 开发工具的安全使用。

应该通过代码评审反馈、文档和可复用流水线控制向开发人员说明安全和质量要求。

随着工具、威胁、架构和组织需求发生变化，政策负责人必须定期评估现有控制是否仍然有效。

## 19. 政策复审触发条件

本政策必须在以下情况下复审：

- 至少每季度一次；
- 发生安全事件后；
- 工作流或架构发生重大变化后；
- 引入身份认证、持久化或后端服务时；
- 新的敏感数据进入项目范围时；
- 部署角色或 OIDC 信任策略发生变化时；
- 引入新的云平台或部署平台时；
- 替换必要安全工具时；
- 适用的组织或监管要求发生变化时。

## 20. 执行

不符合本政策的变更禁止通过正常发布流程，除非已经批准并记录政策例外。

仓库所有者负责确保 GitHub 和 AWS 中实际实施的控制持续与本政策保持一致。
